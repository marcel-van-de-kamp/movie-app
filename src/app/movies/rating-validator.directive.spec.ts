import { RatingValidatorDirective } from './rating-validator.directive';

describe('RatingValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new RatingValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
